/**
 * Static/seed data and data-access helpers.
 *
 * Dynamic content (projects, experience…) will be fetched from Supabase via the
 * clients in `@/lib/supabase`. Local fallback/seed data can live here and be
 * re-exported from this barrel.
 */
export {};
